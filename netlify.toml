[build]
  publish = "."

# 将 /api/chat 路径指向 Edge Function
[[edge_functions]]
  path = "/api/chat"
  function = "chat"

# 这里的 redirects 也可以留着作为兜底
[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200
